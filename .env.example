# Copy to `.env` and fill in real values. docker compose auto-loads `.env`.
# `.env` is gitignored — never commit real secrets.

# --- Database (optional overrides; compose has built-in defaults) ---
# DB_NAME=cv_analyser
# DB_USER=cv_user
# DB_PASSWORD=changeme
