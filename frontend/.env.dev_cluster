# Mode: dev_cluster — connects to the backend running via docker-compose
VITE_APP_MODE=dev_cluster

# URL of the backend container as seen from the host running the Vite dev server.
# Matches the api host port published by docker-compose.dev.yaml (8010 -> 8001).
VITE_DEV_BACKEND_URL=http://localhost:8010
