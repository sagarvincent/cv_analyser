# Mode: production — connects to the live production API
VITE_APP_MODE=production

# Absolute base URL of the production API (no trailing slash).
# The frontend will call ${VITE_API_BASE_URL}/analyze, etc.
VITE_API_BASE_URL=https://api.example.com
